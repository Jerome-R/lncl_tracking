<div class="connection">
    {% if invalid_username is defined %}
        <div class="alert alert-danger">
            <p style="color:white;">{{ 'resetting.request.invalid_username'|trans({'%username%': invalid_username}, 'FOSUserBundle') }}</p>
        </div>
    {% endif %}
    {% if error is defined %}
        <div class="alert alert-danger">{{ error|trans }}</div>
    {% endif %}
    <div class="login-block row">
        {# Le formulaire, avec URL de soumission vers la route « login_check » comme on l'a vu #}
        <div class="block col-md-10 col-md-offset-1 col-xs-12"> 
            <form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="form-inline col-xs-12">
                <div class="form-group col-xs-10">
                    <h2>Réinitialisation du mot de passe</h2>
                    <p style="color:white;">
                        Vous avez perdu votre mot de passe ou celui-ci a expiré. Veuillez renseigner votre adresse email ci-dessous. Nous vous enverrons un email pour réinitialiser votre mot de passe.
                    </p>
                </div>
                <div class="form-group  col-xs-12">
                    <label for="username">Envoyer l'email à l'adresse suivante :</label><br />
                    <div class="input-group col-xs-12">
                        <div class="input-group-addon"><span class="glyphicon glyphicon-user" aria-hidden="true"></span></div>
                        <input type="text" class="form-control" id="username" name="username" placeholder="Email" required="required">
                    </div>
                </div>
                <div class="form-group  col-xs-12">
                    <input type="submit" class="btn btn-home-box-1 pull-right" value="{{ 'resetting.request.submit'|trans({}, 'FOSUserBundle') }}" />
                </div>
            </form>
        </div>
    </div>       
</div>